# =============================================================================
# Stax – Environment variables
# =============================================================================
# Copy this file to .env and fill in your values. Do not commit .env.
# All EXPO_PUBLIC_* vars are baked into the app at build/start time.
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase (required for sign-up / sign-in)
# -----------------------------------------------------------------------------
# Create a project at https://supabase.com → New project.
# In the dashboard: Project Settings → API → copy "Project URL" and "anon public" key.
# Without these, the app runs in guest mode (no login screen, no accounts).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# -----------------------------------------------------------------------------
# Pricing (optional – app works with demo/fallback values if unset)
# -----------------------------------------------------------------------------
# Finnhub – stocks & ETFs: symbol search, quote (latest, previous close, daily change %).
# Free tier has per-minute rate limits. Get a key at https://finnhub.io/register
# When set, used for stock/ETF quotes and Add Asset symbol search; otherwise Alpha Vantage is used for quotes.
EXPO_PUBLIC_FINNHUB_API_KEY=

# Alpha Vantage – stocks & ETFs (fallback if Finnhub key not set). Get a free key at https://www.alphavantage.co/support/#api-key
EXPO_PUBLIC_ALPHA_VANTAGE_API_KEY=

# CoinGecko – crypto (and gold) prices. No key = free tier (rate limited). Pro key = higher limits.
# Get a key at https://www.coingecko.com/en/api/pricing
# EXPO_PUBLIC_COINGECKO_API_KEY=

# Ethplorer – crypto wallet import (Ethereum). Optional; app uses free key if unset (rate limited).
# Get a personal key at https://ethplorer.io/wallet/#screen=api for higher limits.
# EXPO_PUBLIC_ETHPLORER_API_KEY=

# Etherscan – cost basis from on-chain history (tokentx, txlist). Optional; cost basis uses market value at receipt if unset.
# Get a key at https://etherscan.io/apis
# EXPO_PUBLIC_ETHERSCAN_API_KEY=
# EXPO_PUBLIC_ETHERSCAN_API_URL=https://api.etherscan.io/api

# -----------------------------------------------------------------------------
# RevenueCat (optional – paywall is hidden / free-only if unset)
# -----------------------------------------------------------------------------
# Create an app at https://app.revenuecat.com and add iOS/Android apps to get API keys.
# Use the *public* API keys (not secret) for the SDK.
EXPO_PUBLIC_REVENUECAT_API_KEY_IOS=
EXPO_PUBLIC_REVENUECAT_API_KEY_ANDROID=
